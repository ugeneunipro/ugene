// Qt6 port: redirect to Qt's real private QObjectPrivate header.
// The original Qt5-era copy is incompatible with Qt6 (version enum, QObjectUserData removal, etc).
#pragma once
#include <private/qobject_p.h>
