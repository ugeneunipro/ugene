set(UGENE_PLUGIN_ID CoreTests)
set(UGENE_PLUGIN_NAME "Tests for core UGENE functionality")

set(CMAKE_AUTOMOC_MOC_OPTIONS -nn) # Do not display notes.

include(${CMAKE_SOURCE_DIR}/cmake/Plugin.cmake)
